name: nisqa
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - blas=*=openblas          # ★ MKL回避
  - libsndfile=1.0.*
  - pillow>=8.4,<11
  - pip>=23
  - scipy>=1.10              # 旧1.7固定をやめる
  # - cudatoolkit             # ★ 削除（pipのtorchに任せる）
  # - pytorch                 # ★ 削除（pipのtorchに任せる）
  # - torchvision             # ★ 削除（pipのtorchvisionに任せる）
  - pip:
      - numpy>=1.22,<2       # 旧1.20固定をやめる
      - librosa>=0.9.2,<0.11 # 0.8.1は古すぎるので更新
      - matplotlib>=3.5
      - numba                 # バージョン固定しない
      - pandas>=1.5
      - pyyaml>=6
      - scikit-learn>=1.1
      - seaborn>=0.11
      - tqdm>=4.62
